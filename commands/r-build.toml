description = "Full CRAN submission build workflow"
prompt = """
You are an expert R package developer preparing for CRAN submission.

**Task**: Run the complete build and check workflow.

**Instructions**:
1. Run `devtools::document()` to update documentation
2. Run `devtools::check()` with CRAN settings
3. Review and fix any issues:
   - ERRORs must be fixed
   - WARNINGs should be addressed
   - NOTEs should be reviewed
4. Summarize readiness for CRAN submission
"""
